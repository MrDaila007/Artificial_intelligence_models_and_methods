# Лабораторная работа №2

## Тема: Задача распознавания образов с обучением

### Цель работы
Изучение и реализация алгоритмов распознавания образов с обучением: построение классификаторов на основе обучающей выборки и их тестирование на контрольной выборке.

---

## Теоретическая часть

### Шаг 0 (предварительный): Разбиение выборки X⁰ на две части

Выборка X⁰ разбивается на:
- **X⁰_обуч** — обучающая выборка
- **X⁰_контр** — контрольная выборка

**Условия разбиения:**
```
X⁰ᵢ(о) = X⁰_обуч ∩ X⁰ᵢ ≠ ∅,  ∀i ∈ {1, ..., l}
X⁰ᵢ(к) = X⁰_контр ∩ X⁰ᵢ ≠ ∅,  ∀i ∈ {1, ..., l}
```

**Ограничение:** tᵢ / mᵢ ≥ 0.2 (соотношение контрольной и обучающей выборок)

**Обозначения:**
- |X⁰ᵢ(о)| = mᵢ — размер i-го класса в обучающей выборке
- |X⁰ᵢ(к)| = tᵢ — размер i-го класса в контрольной выборке
- ∑mᵢ = m — общий размер обучающей выборки
- ∑tᵢ = t — общий размер контрольной выборки

---

## Алгоритм распознавания A

### Шаг 1: Определение функции попарного сравнения

```
s: X × X → ℝ
```

**ВАРИАНТ I (для пространства X ⊆ ℝⁿ):**

- **Метрика Евклида:**
  ```
  s(x₁, x₂) = √(∑(x₁ᵢ - x₂ᵢ)²)
  ```

- **Метрика Минковского (p ∈ ℕ):**
  ```
  s(x₁, x₂) = (∑|x₁ᵢ - x₂ᵢ|ᵖ)^(1/p)
  ```

**ВАРИАНТ II (для пространства X ⊆ 𝔹ⁿ):**

- **Метрика Хэмминга:**
  ```
  s(x₁, x₂) = ∑|x₁ᵢ - x₂ᵢ|
  ```

### Шаг 2: Определение функции сравнения с классами

```
fᵢ: ℝᵐ → ℝ,  i ∈ {1, ..., l}
```

**Варианты реализации:**

1. **Среднее расстояние до класса Xᵢ:**
   ```
   fᵢ(x) = (mᵢ)⁻¹ × ∑ s(x, xⱼ),  где xⱼ ∈ X⁰ᵢ(о)
   ```

2. **k ближайших соседей:**
   - Получить набор s(X⁰ᵢ(о)) = {s(x, x₁), ..., s(x, xₘᵢ)}
   - Переупорядочить по возрастанию
   - Взять k первых элементов: X̄⁰ᵢ(о)
   - Вычислить:
   ```
   fᵢ(x) = (k)⁻¹ × ∑ s(x, xⱼ),  где xⱼ ∈ X̄⁰ᵢ(о)
   ```

3. **Минимальное расстояние до объектов класса Xᵢ:**
   ```
   fᵢ(x) = min s(x, xⱼ),  где xⱼ ∈ X⁰ᵢ(о)
   ```

### Шаг 3: Определение решающего правила

```
Pᴬ: ℝˡ → 𝔹ˡ₂,  где 𝔹₂ = {0, 1}
```

**По минимуму оценки до класса Xᵢ:**
```
PᵢA(x) = 1, если fᵢ(x) = min{f₁(x), ..., fₗ(x)}
PᵢA(x) = 0, в противном случае
```

Вектор Pᴬ(x) = (P₁ᴬ(x), ..., Pₗᴬ(x)) называется **классификационным вектором**.

### Шаг 4: Тестирование алгоритма

**Инициализация:** t⁰(X⁰_контр) = 0

**Шаг 4.1:** Для каждого объекта x ∈ X⁰_контр:
1. Вычислить вектор (s(x, x₁), ..., s(x, xₘ))
2. Вычислить вектор (f₁(x), ..., fₗ(x))
3. Вычислить классификационный вектор Pᴬ(x)
4. Если Pᴬ(x) = P(x), то t⁰ = t⁰ + 1

**Шаг 4.2:** Вычислить функционал качества:
```
Φᴬ(X⁰_контр) = t⁰(X⁰_контр) / t  ∈ [0, 1]
```

---

## Задание

1. Выбрать датасет из предложенного списка
2. Разбить выборку на обучающую и контрольную части
3. Реализовать алгоритм распознавания с обучением
4. Протестировать различные комбинации функций (шаги 1-3)
5. Вычислить функционал качества для каждой комбинации
6. Создать графический интерфейс для визуализации результатов

---

## Требования к программному продукту

1. **Любой язык программирования** (рекомендуется Python)
2. **Графический интерфейс обязателен**
3. **Вывод на экран по запросу:**
   - Результаты классификации
   - Функционал качества Φᴬ для каждого разбиения
   - Сравнительная таблица результатов

---

## Практическая часть

### Структура реализации
- `solution.py` — содержит класс `PatternRecognitionClassifier`, функцию `run_experiment` и CLI/GUI для запуска эксперимента.
- `app.py` — Streamlit-приложение для загрузки набора данных, настройки метрик (евклидова, Минковского, Хэмминга), выбора функции сравнения и отображения статистики по Φᴬ.
- Общие искусственные и реальные CSV располагаются в каталоге `data/`, что позволяет быстро протестировать алгоритм.

### Подготовка окружения
```bash
cd Artificial_intelligence_models_and_methods
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Запуск
- **Консоль / tkinter**  
  ```bash
  python Lab2/solution.py --input data/iris.csv --metric euclidean --comparison mean --test-size 0.2
  ```  
  Скрипт автоматически разобьёт данные на обучающую/контрольную части, обучит выбранный классификатор и выведет Φᴬ.

- **Web-интерфейс**  
  ```bash
  streamlit run Lab2/app.py
  ```  
  В браузере доступны интерактивное разбиение выборки, анализ по всем комбинациям функций и визуализация.

### Формат входных данных
- CSV с заголовком; все числовые признаки приводятся к числовому типу (float или int).
- Последний столбец обычно содержит метки классов (строки или числа). В интерфейсе можно выбрать любой столбец как целевую переменную.
- Для корректного стратифицированного разделения следует иметь минимум по 5–10 объектов каждого класса.

### Проверка работоспособности
Запустите интеграционные тесты из корня проекта:
```bash
./venv/bin/python test_web.py
```
Сценарий создаёт синтетические данные, обучает `PatternRecognitionClassifier`, тестирует `run_experiment` и убеждается, что вычисление Φᴬ стабильно.

---

## Таблица результатов

| Алгоритм | {X⁰_обуч, X⁰_контр}₁ | {X⁰_обуч, X⁰_контр}₂ |
|----------|----------------------|----------------------|
| A        | Φᴬ₁                   | Φᴬ₂                   |

---

## Рекомендуемые датасеты

- [Биткойн, исторические данные](https://www.kaggle.com/mczielinski/bitcoin-historical-data)
- [FIFA 19 полный набор данных игроков](https://www.kaggle.com/karangadiya/fifa19)
- [Рентгенография грудной клетки (пневмония)](https://www.kaggle.com/paultimothymooney/chest-xray-pneumonia)
- [Датасет болезней сердца](https://www.kaggle.com/ronitf/heart-disease-uci)
- [Распознавание пола по голосу](https://www.kaggle.com/primaryobjects/voicegender)
- [Качество красного вина](https://www.kaggle.com/uciml/red-wine-quality-cortez-et-al-2009)
- [Обнаружение мошенничества с кредитными картами](https://www.kaggle.com/mlg-ulb/creditcardfraud)
- [Набор данных цифр языка жестов](https://www.kaggle.com/ardamavi/sign-language-digits-dataset)

---

## Контрольные вопросы

1. Как формируются обучающая и контрольная выборки?
2. Какие метрики используются для сравнения объектов в пространстве ℝⁿ и 𝔹ⁿ?
3. Опишите метод k ближайших соседей.
4. Что такое классификационный вектор?
5. Как вычисляется функционал качества алгоритма?
6. В чём преимущества и недостатки различных функций сравнения с классами?
